<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar</title>
    <style>
      label {
        display: block;
        margin-bottom: 8px;
      }

      input {
        margin-bottom: 16px;
        padding: 10px;
        width: 500px;
      }
      input::-webkit-outer-spin-button,
      input::-webkit-inner-spin-button {
        -webkit-appearance: none;
        margin: 0;
      }

      textarea{
        margin-bottom: 1rem;
        padding: 10px;
      }
      .flex{
        display: flex;
        gap: 1rem;
        align-items: center;
        margin-bottom: 1rem;
      }

      #recommendation{
        width: 30px;
      }

      #isbn{
        width: 100px;
      }
      
      #form_btn{
        padding: 10px;
        margin-left: 10px;
      }

    </style>
  </head>

  <body>
    <h2>Editar Nuevo Libro</h2>

    <form id="bookForm" method="post" action="/edit/<%= data.id %>">
      <label for="title">Título:</label>
      <input type="text" id="title" name="title" value="<%= data.title %>" required />

      <label for="author">Autor:</label>
      <input type="text" id="author" name="author"  value="<%= data.author %>" required />

      <div class="flex">
        <label for="recommendation">Recomendación:</label>
      <input type="number" id="recommendation" name="recommendation" pattern="[1-9]|10" maxlength="4" size="4"  value="<%= data.recommendation %>" required></input>
      </div>

      <label for="review">Reseña:</label>
      <textarea id="review" name="review" rows="8" cols="70" required></textarea>

      <label for="isbn">ISBN:</label>
      <input
        type="text"
        id="isbn"
        name="isbn"
        pattern="[0-9]{13}"
        title="El ISBN debe contener 13 dígitos numéricos"
         value="<%= data.isbn %>" required
      />

      <button id="form_btn" type="submit">Editar Libro</button>

    </form>
  </body>
  <script>
    document.getElementById("review").value ="<%= data.review %>";

  </script>
</html>
